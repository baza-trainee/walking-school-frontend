---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/walkings/repositories/walking-school-frontend/
    - /bin/bash -c 'npm install'
    - /bin/bash -c 'npm run build'
    - /bin/cp -r ./build/* $DEPLOYPATH
