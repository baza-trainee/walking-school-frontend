on:
  push:
    branches:
      - dev
name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: Use Node.js 18
        uses: actions/setup-node@v2
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: 🔨 Build Project
        run: |
          npm run build

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.walking-school.site
          username: front@walking-school.site
          password: ${{ secrets.GIT_CPANEL_REACT_PASSWORD }}
          local-dir: ./build/
